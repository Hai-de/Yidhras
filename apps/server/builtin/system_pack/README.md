# Yidhras System Pack

内置系统插件，提供核心数据清洗能力。启动时自动加载，无需手动激活。

## 包含的插件

- **string-methods** — 基础字符串清洗方法（trim、lowercase、collapse_ws 等）
- **regex-engine** — 正则表达式模式匹配与替换

## 注意

此 pack 由 Yidhras 内核维护，不要手动修改其中的插件。
