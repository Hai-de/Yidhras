import type { Express } from 'express';

import type { AppContext } from '../context.js';

export const registerInferenceRoutes = (_app: Express, _context: AppContext): void => {
  void _app;
  void _context;
};
