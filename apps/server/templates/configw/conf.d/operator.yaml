# 操作员认证与账户配置
# 生产环境务必通过环境变量或 local.yaml 覆盖 jwt_secret 和 default_password。
operator:
  auth:
    jwt_secret: "changeme-please-replace-with-a-secure-random-string"
    jwt_expires_in: "24h"
    bcrypt_rounds: 12
  root:
    default_password: "changeme-root-password"
