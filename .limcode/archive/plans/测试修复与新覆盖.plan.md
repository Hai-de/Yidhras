<!-- LIMCODE_SOURCE_ARTIFACT_START -->
{"type":"design","path":".limcode/design/ai-registry-hot-reload.md","contentHash":"sha256:081785ac5c1aaa8dc82ca70f5318dfd9bee993d42f5fccd3ba4c73bf8574f860"}
<!-- LIMCODE_SOURCE_ARTIFACT_END -->

## TODO LIST

<!-- LIMCODE_TODO_LIST_START -->
- [x] 修复 prompt_bundle_v2.spec.ts — 删除 T2 和 T6  `#t1`
- [x] 修复 prompt_workflow_sections.spec.ts — 删除过时测试  `#t2`
- [x] 新建 registry.spec.ts — YAML 加载/合并/zod 校验测试  `#t3`
- [x] 新建 task_decoder.spec.ts — decodeAiTaskOutput 各模式测试  `#t4`
- [x] 扩展 observability.spec.ts — 降级行为测试  `#t5`
- [x] 新建 openai_adapter.spec.ts — 请求构建 + 响应解析测试  `#t6`
- [x] 更新 TODO.md — 标记测试覆盖完成  `#t7`
<!-- LIMCODE_TODO_LIST_END -->

# 测试修复与新覆盖 实施计划

## 范围

1. 修复 3 个预存测试失败（`buildPromptBundle` / `toLegacyPromptBundle` 已删除但测试未同步）
2. 为 TODO.md 列出的网关模块补充新测试
3. 更新 TODO.md

---

## T1: 修复 `tests/unit/prompt_bundle_v2.spec.ts` — 删除 2 个过时测试

**删除的测试**:
- `T2: toLegacyPromptBundle produces compatible PromptBundle structure` — 引用的 `toLegacyPromptBundle` 已从 `prompt_bundle_v2.ts` 删除
- `T6: old buildPromptBundle still works unchanged` — 引用的 `buildPromptBundle` 已从 `prompt_builder.ts` 删除，旧函数已被 `buildPromptBundleV2` 替代

**操作**: 删除这两个 `it(...)` 块，约 50 行。

---

## T2: 修复 `tests/unit/prompt_workflow_sections.spec.ts` — 重写过时测试

**问题**: `records task-aware section summaries in prompt bundle metadata` 使用了已删除的 `buildPromptBundle(context, { task_type })`。

**重写**: 改用新 API `buildPromptTree(ctx, registry)` + `buildPromptBundleV2(tree, ctx)`，保留原有的 section summary 断言逻辑。registry 从测试中构建或引入 SIMPLE_SLOT_REGISTRY 复用。

---

## T3: 新建 `tests/unit/registry.spec.ts` — AI 注册表 YAML 加载/合并

**覆盖**:
- `loadAiRegistryConfig()` 加载内置默认 + 空文件覆盖 → 使用内置配置
- `mergeAiRegistryConfig()` 合并逻辑: providers 合并、models 合并（同 provider:model 覆盖）、routes 合并（同 route_id 覆盖）
- `aiRegistryConfigSchema.parse()` 校验合法/非法输入
- `resetAiRegistryCache()` + 再次 get 会重新加载

**注意**: 测试中可能需要 mock `readYamlFileIfExists` 或使用内存临时文件。优先不 mock，用内置默认配置的纯函数测试（merge 函数是纯函数，可直接测）。

---

## T4: 新建 `tests/unit/task_decoder.spec.ts` — schema 校验/alias/unwrap

**覆盖**:
- `decodeAiTaskOutput()` 各响应模式: json_object → parse JSON；json_schema → 通过 schema 的 json；free_text → 返回字符串
- alias 解析（如果存在)
- unwrap 逻辑（嵌套路径提取）
- 边界: 空输出、非 JSON 文本在 json 模式下的错误处理

---

## T5: 新建 `tests/unit/observability.spec.ts` — Prisma 写入/降级

**覆盖**（复用现有 `tests/unit/observability.spec.ts` 的已有测试风格）:
- `recordAiInvocation()` 正常写入 → Prisma upsert 被调用
- `recordAiInvocation()` 在 context 为 null 时不崩溃（降级 warn，不抛）
- Prisma write 失败时 catch+log（不抛）
- latency_ms 计算: 优先 `usage.latency_ms`，回退到 attempts 累加
- source_inference_id 传递

**注意**: 现有 `tests/unit/observability.spec.ts` 已有 10 个测试全部通过。这些新增测试补充降级行为覆盖。

---

## T6: 新建 `tests/unit/openai_adapter.spec.ts` — OpenAI adapter 请求构建 + 响应解析

**覆盖**:
- 请求构建: `buildChatCompletionsRequestBody()` / `buildResponsesRequestBody()` 正确映射 messages、tools、response_format
- 响应解析: `normalizeChatCompletionsResponse()` 正确提取 text/tool_calls
- 响应解析: `normalizeResponsesApiResponse()` 正确提取 output text/tool_calls
- tool calling: 当 tools 非空时 `buildOpenAiTools()` 正确生成 tool spec
- 鉴权: `resolveApiKey()` 从 env 读取，缺失时返回 null → `buildAuthMissingResult()`
- 错误响应: `parseErrorPayload()` 从 error response 提取 code/message

---

## T7: 更新 TODO.md

将测试覆盖条目标记为 `[x]`。

---

## 文件变更总览

| 文件 | 操作 | 规模 |
|------|------|------|
| `tests/unit/prompt_bundle_v2.spec.ts` | 修改（删 2 个 it） | -50 行 |
| `tests/unit/prompt_workflow_sections.spec.ts` | 修改（重写 1 个 it） | ~20 行 |
| `tests/unit/registry.spec.ts` | **新建** | ~80 行 |
| `tests/unit/task_decoder.spec.ts` | **新建** | ~60 行 |
| `tests/unit/observability.spec.ts` | 修改（追加） | ~50 行 |
| `tests/unit/openai_adapter.spec.ts` | **新建** | ~100 行 |
| `TODO.md` | 修改 | 1 行 |

---

## 不涉及

- 不新增集成/E2E 测试（HTTP 路由契约测试留到后续）
- 不修改任何 src/ 源码
- 不添加依赖
