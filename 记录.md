# Lint Debt Record / 历史告警记录

Purpose / 用途:

- Track existing lint warnings/errors before formal cleanup.
- 记录正式开发前需要处理的历史 lint 问题。

Policy / 规则:

- Do not silently fix broad historical debt in feature PRs.
- 功能开发过程中不要顺手大面积重构历史警告。
- New code should not introduce avoidable new warnings.
- 新增代码应尽量不引入可避免的告警。

---

## Round 2 Draft Scope / 第二轮草案范围

- This round provides a planning-grade breakdown only.
- 本轮仅提供可执行规划，不进行告警清零改动。
- Goal: convert baseline warning counts into prioritized work items.
- 目标：将基线告警转换为按优先级排序的工作清单。

## Baseline Snapshot (2026-03-20)

### apps/server

- Command / 命令: `npm run lint --prefix apps/server`
- Result / 结果: 0 errors, 29 warnings

#### Warning by Category / 按类别统计

- `simple-import-sort/imports`: 8
- `@typescript-eslint/no-explicit-any`: 10
- `no-case-declarations`: 7
- `@typescript-eslint/no-unused-vars`: 2
- `prefer-const`: 1
- Total: 29

#### Warning by File / 按文件统计

- `apps/server/src/index.ts`: 10
  - import sort (1)
  - no-explicit-any (7)
  - no-unused-vars (1)
- `apps/server/src/dynamics/algorithms.ts`: 7
  - no-case-declarations (7)
- `apps/server/src/narrative/resolver.ts`: 4
  - import sort (1)
  - no-explicit-any (3)
- `apps/server/src/world/loader.ts`: 3
  - import sort (1)
  - no-explicit-any (2)
- `apps/server/src/clock/engine.ts`: 2
  - prefer-const (1)
  - no-unused-vars (1)
- `apps/server/src/core/simulation.ts`: 1 (import sort)
- `apps/server/src/db/seed.ts`: 1 (import sort)
- `apps/server/src/dynamics/manager.ts`: 1 (import sort)
- `apps/server/src/world/test.ts`: 1 (import sort)

### apps/web

- Command / 命令: `npm run lint --prefix apps/web`
- Result / 结果: 0 errors, 2 warnings

#### Warning by Category / 按类别统计

- `@typescript-eslint/no-explicit-any`: 2

#### Warning by File / 按文件统计

- `apps/web/stores/clock.ts`: 2

---

## Prioritized Cleanup Queue / 优先级清理队列

### P0 - Rule-Intent Integrity / 规则意图一致性

Goal / 目标:

- Keep rule semantics clear before full development starts.
- 在正式开发前，确保规则含义与团队约定一致。

Items / 条目:

- [ ] Add explicit inline justification comments for unavoidable `any` usage in touched files.
- [ ] Use stricter typing for obvious low-risk `any` cases first (store/API payloads).

Exit Criteria / 完成标准:

- Any remaining `any` in touched files has a short rationale comment.
- 所有改动触及文件中保留的 `any` 都有简短注释说明。

### P1 - Deterministic Imports / 导入顺序一致

Goal / 目标:

- Remove low-risk import-order noise to improve diff readability.
- 清理低风险导入排序告警，提升 diff 可读性。

Items / 条目:

- [ ] Resolve 8 `simple-import-sort/imports` warnings in server files.
- [ ] Keep import grouping: external first, local second.

Exit Criteria / 完成标准:

- `simple-import-sort/imports` warnings drop from 8 to 0.

### P2 - Safety Warnings / 语义安全告警

Goal / 目标:

- Fix warnings that can hide future runtime bugs.
- 修复可能掩盖运行时问题的告警。

Items / 条目:

- [ ] Refactor `switch` case declarations in `apps/server/src/dynamics/algorithms.ts`.
- [ ] Resolve unused variable warnings in `apps/server/src/clock/engine.ts` and `apps/server/src/index.ts`.
- [ ] Replace mutable declarations with `const` where applicable.

Exit Criteria / 完成标准:

- `no-case-declarations`, `no-unused-vars`, `prefer-const` warnings are 0 in server.

### P3 - Any Reduction Sprint / any 收敛冲刺

Goal / 目标:

- Reduce `any` usage while preserving delivery speed.
- 在不拖慢开发节奏前提下收敛 `any`。

Items / 条目:

- [ ] Server high-impact files: `apps/server/src/index.ts`, `apps/server/src/narrative/resolver.ts`, `apps/server/src/world/loader.ts`.
- [ ] Web file: `apps/web/stores/clock.ts`.
- [ ] Introduce minimal local types/interfaces for API responses and error objects.

Exit Criteria / 完成标准:

- `no-explicit-any` warnings reduced by at least 60% from baseline (12 -> <= 5).

---

## Suggested Execution Order / 建议执行顺序

1. P1 imports cleanup (fast, low risk)
2. P2 semantic warnings (localized refactors)
3. P3 any reduction with comment policy
4. Re-run lint + typecheck and update this file

## Verification Commands / 验证命令

- `npm run lint --prefix apps/server`
- `npm run lint --prefix apps/web`
- `npm run typecheck --prefix apps/server`
- `npm run typecheck --prefix apps/web`

## Change Log / 变更记录

- 2026-03-20: Added Round 2 draft breakdown with per-file priorities.
- 2026-03-20: Added world-pack time step config `simulation_time.step_ticks` (cyber_noir), server loop now reads this value with fallback to `1` when invalid.
- 2026-03-20: Follow-up noted for polishing stage: current loop fetches step size via in-memory getter each tick (not file I/O); consider introducing a centralized runtime speed policy and hot-reload strategy only when dynamic rate switching is required.
- 2026-03-21: Captured previously undefined acceptance items for startup smoke and API consistency; aligned policy to treat degraded startup as pass for fresh clones.

## Requirement Alignment Notes (2026-03-21)

Purpose / 用途:

- Record scope/acceptance items that were previously implicit during smoke-test rollout.
- 记录冒烟测试落地过程中此前未显式定义的验收边界，避免后续口径漂移。

### A1. Degraded Startup Acceptance / 降级启动验收

- Decision / 决策:
  - `health_level=degraded` and `runtime_ready=false` is treated as acceptable startup state for first-time clones.
  - 首次拉取仓库时内容可能为空，降级启动视为通过，不作为冒烟失败条件。
- Enforcement / 落地:
  - Startup and endpoint smoke scripts accept degraded path and validate structured responses in both branches.

### A2. Runtime-Not-Ready API Consistency / 运行时未就绪接口一致性

- Problem / 问题:
  - `/api/clock/formatted` previously returned `500/CLOCK_FORMAT_ERR` when runtime was not ready, inconsistent with other world-pack-gated endpoints.
- Decision / 决策:
  - Runtime-not-ready path must return `503/WORLD_PACK_NOT_READY` consistently.
- Enforcement / 落地:
  - Route behavior aligned in server entrypoint.
  - Smoke assertions updated accordingly.
  - API.md updated to document split between not-ready (`WORLD_PACK_NOT_READY`) and real formatting failure (`CLOCK_FORMAT_ERR`).

### A3. Startup Flow Single Source / 启动流程单一入口

- Problem / 问题:
  - Runtime prerequisites were scattered across dev scripts, CI steps, and manual commands.
- Decision / 决策:
  - Introduce unified server script `prepare:runtime` as the single startup preparation entry.
- Enforcement / 落地:
  - Reused in Linux/Windows start scripts and GitHub Actions smoke workflow.

### A4. Explicit Hard Requirements / 硬性要求显式化

- Decision / 决策:
  - Document non-negotiable startup/testing requirements in README:
    - Run `prepare:runtime` before startup.
    - Degraded startup is acceptable for empty fresh-clone data state.
    - World-pack gated endpoints return `503/WORLD_PACK_NOT_READY` when runtime is not ready.

---

## Cleanup Progress Snapshot (2026-03-20)

### Results / 结果

- `npm run lint --prefix apps/server`: 0 errors, 0 warnings
- `npm run lint --prefix apps/web`: 0 errors, 0 warnings
- `npm run typecheck --prefix apps/server`: pass
- `npm run typecheck --prefix apps/web`: pass

### Completed Items / 已完成项

- [x] P1 import ordering warnings removed.
- [x] P2 semantic warnings removed (`no-case-declarations`, `no-unused-vars`, `prefer-const`).
- [x] P3 `any` warnings removed in touched server/web files.

### Notes / 备注

- Resolver variable typing was upgraded to support nested values via recursive `VariableValue` type.
- Error handling in server entrypoints now uses `unknown` + safe message extraction helper.

## Rule Hardening Snapshot (2026-03-20)

- Promoted to error in server/web:
  - `@typescript-eslint/no-explicit-any`
  - `@typescript-eslint/no-unused-vars`
  - `simple-import-sort/imports`
  - `simple-import-sort/exports`
- Promoted to error in server:
  - `prefer-const`
  - `no-case-declarations`
- Verification after hardening:
  - `npm run lint --prefix apps/server`: pass
  - `npm run lint --prefix apps/web`: pass

---

## Next-Stage Candidate Rules (Warn First) / 下一阶段候选规则（先警告）

Purpose / 用途:

- These rules are candidates for pre-kickoff evaluation.
- 这些规则用于开工前评估，不在当前阶段直接收紧为 error。

### Shared Candidates (Server + Web) / 前后端通用候选

- `@typescript-eslint/consistent-type-imports`: `warn`
  - Why: keeps type-only imports explicit and reduces runtime-import confusion.
  - 理由：明确类型导入，降低运行时导入歧义。
- `eqeqeq`: `warn`
  - Why: avoids loose equality edge cases.
  - 理由：避免宽松比较带来的边界问题。
- `curly`: `warn`
  - Why: prevents accidental single-line branch bugs.
  - 理由：减少单行分支引发的误判和维护风险。

### Server-Focused Candidates / 后端候选

- `@typescript-eslint/no-floating-promises`: `warn` (evaluate runtime noise first)
  - Why: helps catch unhandled async flows in API/service paths.
  - 理由：帮助发现 API/服务链路中未处理的异步调用。
- `@typescript-eslint/await-thenable`: `warn`
  - Why: catches invalid `await` usage.
  - 理由：识别错误的 `await` 用法。

### Web-Focused Candidates / 前端候选

- `vue/no-mutating-props`: `warn`
  - Why: avoids hidden state coupling in component tree.
  - 理由：避免组件树中的隐性状态耦合。
- `vue/no-setup-props-destructure`: `warn` (only if team wants conservative reactivity safety)
  - Why: reduces accidental reactivity loss in setup usage.
  - 理由：降低 setup 中解构 props 导致响应性丢失的风险。

### Not Recommended to Dual-Enable / 不建议重复启用

- `import/order` is not recommended while using `simple-import-sort`.
- 在已启用 `simple-import-sort` 的前提下，不建议再启用 `import/order`，避免规则冲突。

## Pre-Kickoff Evaluation Checklist / 开工前评估清单

- [ ] Enable candidate rules as `warn` in a short trial branch.
- [ ] Run lint on both apps and capture warning volume.
- [ ] Label noisy/low-value rules for deferment.
- [ ] Promote only stable, low-noise candidates to `error`.
- [ ] Update `AGENTS.md` and this file with final policy.
