# Engineering Verification Record / 工程验证记录

Purpose / 用途:

- Keep concise verification snapshots, acceptance notes, and quality evidence.
- 仅保留精简的验证快照、验收边界与质量证据。
- Current project status and priorities belong to `TODO.md`.
- 当前项目状态与优先级以 `TODO.md` 为准。
- Detailed API/architecture/logic explanations belong to `docs/`.
- 详细接口、架构、逻辑说明以 `docs/` 下文档为准。

Policy / 规则:

- Keep this file snapshot-oriented and evidence-focused.
- 本文件以快照与证据为主，避免演变为“流水账”。
- Record only what has been verified, accepted, or observed.
- 这里只记录已经验证、验收或明确观察到的内容。
- Do not turn this file into a milestone board or design log.
- 不要把这里扩写成里程碑总表或设计日志。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Phase 3 Closure Completed)

### Delivered / 已交付

- The scheduler Phase 3 closure pass is now complete instead of remaining as an open cleanup plan.
- scheduler Phase 3 收尾现已完成，不再停留在待处理的清理计划状态。
- Candidate evaluation now goes through a more explicit readiness layer, so `pending_workflow` / recovery-window suppression / cooldown / idempotency checks are no longer only scattered inline conditions.
- candidate evaluation 现已通过更明确的 readiness 层组织，`pending_workflow` / recovery-window suppression / cooldown / idempotency 等判断不再只是散落的内联条件。
- Event-driven merge explainability is now queryable through scheduler decision read models via derived coalescing fields, while `event_coalesced` remains a summary-side taxonomy signal instead of being forced into a fake candidate-level skip reason.
- event-driven 合并解释性现在已经可通过 scheduler decision read model 上的派生 coalescing 字段稳定查询；同时 `event_coalesced` 继续保留为 summary-side taxonomy 信号，而没有被强行伪装成 candidate-level skip reason。
- Scheduler cursor/watermark semantics are now tighter: `last_signal_tick` no longer blindly advances to `now`, but advances from the latest actually observed signal / recovery watermark for the partitioned run.
- scheduler cursor/watermark 语义现已收紧：`last_signal_tick` 不再盲目推进到 `now`，而是基于 partition 运行中实际观测到的最新 signal / recovery watermark 前进。
- Lease acquisition/update paths were also hardened for partition-level contention so failover and automatic rebalance compatibility paths keep working under racing workers.
- lease 的 acquire/update 路径也已针对 partition 竞争做了加固，因此 failover 与 automatic rebalance compatibility 路径在 worker 竞争下仍可保持正常工作。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass
- `pnpm --filter yidhras-server test:agent-scheduler-projection`: pass
- `pnpm --filter yidhras-server test:scheduler-queries`: pass
- `pnpm --filter yidhras-server test:scheduler-failover`: pass
- `pnpm --filter yidhras-server test:scheduler-migration-failover-compatibility`: pass
- `pnpm --filter yidhras-server test:scheduler-automatic-rebalance-failover-compatibility`: pass

### Note / 说明

- This closure pass intentionally stops at a stronger baseline; it does not introduce a new DSL policy system, control-plane mutation API, or a heavier distributed scheduling architecture.
- 本轮收尾刻意停在“更强基线”层面；没有引入新的 DSL policy system、control-plane mutation API 或更重的 distributed scheduling architecture。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Phase 3 Closure Planning Sync)

### Delivered / 已交付

- Historical Phase 3 full-route plan was removed after it no longer matched the real codebase state, and a narrower closure plan is now the active planning artifact.
- 原始 Phase 3 全量路线文件因不再匹配真实代码状态已移除，当前改由更窄的收尾计划承接后续工作。
- Added `.limcode/plans/agent-scheduler-phase-3-closure.plan.md` to freeze the current scheduler baseline and focus follow-up work on actor readiness, skip taxonomy / event coalescing explainability, cursor/watermark baseline tightening, and documentation sync.
- 已新增 `.limcode/plans/agent-scheduler-phase-3-closure.plan.md`，用于冻结当前 scheduler 已交付基线，并将后续工作收敛到 actor readiness、skip taxonomy / event coalescing explainability、cursor/watermark baseline 收口与文档同步。
- `docs/API.md` and `TODO.md` were re-aligned to treat current scheduler observability / ownership / rebalance capabilities as existing baseline, while clarifying that the next implementation round is the closure pass rather than a replay of the original Phase 3 roadmap.
- `docs/API.md` 与 `TODO.md` 已重新对齐，明确当前 scheduler observability / ownership / rebalance 能力属于既有基线，而下一轮实现将以收尾计划为主，而非重复执行旧版 Phase 3 路线。

### Verification / 验证

- Documentation consistency review against current scheduler routes/services: completed
- 文档一致性复核（对照当前 scheduler routes / services）：已完成

### Note / 说明

- This round is documentation-only; no business code or runtime behavior was changed yet.
- 本轮仅做文档同步，尚未修改业务代码或运行时行为。

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Automatic Rebalance Closure)

### Delivered / 已交付

- Automatic rebalance baseline is now closed with stronger compatibility verification instead of stopping at recommendation/apply observability.
- automatic rebalance 基线现已完成收口，不再停留在 recommendation/apply 可观测性，而是补齐了更强的兼容性验证。
- Added dedicated `scheduler_automatic_rebalance_failover_compatibility` e2e to confirm that automatic rebalance apply remains lease-respecting: applied recommendation does not preempt an active lease, assignment stays `migrating` until lease expiry, and the target worker completes handoff through the existing takeover contract.
- 已新增专门的 `scheduler_automatic_rebalance_failover_compatibility` e2e，确认 automatic rebalance apply 继续保持 lease-respecting：已 applied 的 recommendation 不会抢占 active lease，assignment 会在 lease expiry 前保持 `migrating`，并由目标 worker 通过既有接管契约完成 handoff。
- Cursor continuity is now also explicitly verified on the automatic rebalance path, proving that cursor state survives recommendation apply + lease expiry + owner takeover in one path.
- 当前也已在 automatic rebalance 路径上显式验证 cursor continuity，确认 recommendation apply + lease expiry + owner takeover 的一体化链路不会丢失 cursor 状态。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:scheduler-operator-projection`: pass
- `pnpm --filter yidhras-server test:scheduler-queries`: pass
- `pnpm --filter yidhras-server test:scheduler-automatic-rebalance-apply`: pass
- `pnpm --filter yidhras-server test:scheduler-automatic-rebalance-failover-compatibility`: pass
- `pnpm --filter yidhras-server test:scheduler-runtime-status`: pass
- `pnpm --filter yidhras-server test:scheduler-worker-runtime-state`: pass
- `pnpm --filter yidhras-server test:scheduler-rebalance-recommendation`: pass
- `pnpm --filter yidhras-server test:scheduler-rebalance-suppression`: pass
- `pnpm --filter yidhras-server test:scheduler-ownership-migration`: pass
- `pnpm --filter yidhras-server test:scheduler-rebalance-handoff`: pass
- `pnpm --filter yidhras-server test:scheduler-failover`: pass
- `pnpm --filter yidhras-server test:scheduler-migration-failover-compatibility`: pass

### Note / 说明

- Current closure still keeps automatic rebalance intentionally bounded: recommendation/apply exists, but no broad control-plane mutation API or advanced load-learning strategy is introduced in this round.
- 当前收口仍刻意保持 automatic rebalance 的 bounded 语义：已具备 recommendation/apply，但本轮仍未引入大范围 control-plane mutation API 或高级负载学习策略。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Automatic Rebalance Observability Round)

### Delivered / 已交付

- Scheduler automatic rebalance baseline is now no longer limited to internal recommendation/apply logic; worker runtime health and rebalance recommendation history are now observable through read APIs and operator projection.
- scheduler automatic rebalance 基线现已不再局限于内部 recommendation/apply 逻辑；worker runtime health 与 rebalance recommendation history 现已通过只读 API 与 operator projection 对外可观测。
- Added worker runtime heartbeat/liveness read surface and rebalance recommendation read surface, so operator can inspect `active|stale|suspected_dead` workers and recent `recommended|suppressed|applied|superseded` rebalance history.
- 已新增 worker runtime heartbeat/liveness 读面与 rebalance recommendation 读面，使 operator 可直接检查 `active|stale|suspected_dead` worker 与最近 `recommended|suppressed|applied|superseded` rebalance 历史。
- Scheduler operator projection now also includes `workers`, `rebalance`, and rebalance-aware highlights such as `latest_rebalance_status`, `latest_rebalance_partition_id`, and `latest_stale_worker_id`.
- scheduler operator projection 现已进一步包含 `workers`、`rebalance` 与 rebalance-aware highlights，例如 `latest_rebalance_status`、`latest_rebalance_partition_id` 与 `latest_stale_worker_id`。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:scheduler-rebalance-recommendation`: pass
- `pnpm --filter yidhras-server test:scheduler-rebalance-suppression`: pass
- `pnpm --filter yidhras-server test:scheduler-automatic-rebalance-apply`: pass

### Note / 说明

- Current automatic rebalance baseline still keeps recommendation/apply bounded and lease-respecting; it is not yet a full control-plane or rich load-aware balancing system.
- 当前 automatic rebalance 基线仍保持 bounded recommendation/apply 与 lease-respecting 语义；尚不是完整 control-plane，也不是富负载感知均衡系统。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Ownership Observability Round 4)

### Delivered / 已交付

- Scheduler ownership observability is now no longer limited to `/api/status`; dedicated read-only ownership and migration history APIs are now available.
- scheduler ownership 观测面现已不再局限于 `/api/status`；现已新增只读 ownership 与 migration history API。
- Added `GET /api/runtime/scheduler/ownership` and `GET /api/runtime/scheduler/migrations`, exposing persisted `SchedulerPartitionAssignment` / `SchedulerOwnershipMigrationLog` state for operator diagnostics.
- 已新增 `GET /api/runtime/scheduler/ownership` 与 `GET /api/runtime/scheduler/migrations`，用于向 operator 诊断暴露持久化 `SchedulerPartitionAssignment` / `SchedulerOwnershipMigrationLog` 状态。
- Scheduler operator projection now also includes `ownership: { assignments, recent_migrations, summary }` plus ownership-aware highlights such as `migration_in_progress_count` and `top_owner_worker_id`.
- scheduler operator projection 现已进一步包含 `ownership: { assignments, recent_migrations, summary }`，并补齐 `migration_in_progress_count`、`top_owner_worker_id` 等 ownership-aware highlights。
- Runtime status contract/docs were aligned so `/api/status.scheduler` formally includes `assignment_source` and `migration_in_progress_count`.
- runtime status 契约/文档现已对齐，`/api/status.scheduler` 正式包含 `assignment_source` 与 `migration_in_progress_count`。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:scheduler-operator-projection`: pass
- `pnpm --filter yidhras-server test:scheduler-queries`: pass
- `pnpm --filter yidhras-server test:scheduler-migration-failover-compatibility`: pass

### Note / 说明

- Current delivery still keeps ownership/migration surfaces read-only; mutation/control-plane APIs for rebalance are still intentionally out of scope.
- 当前交付仍将 ownership/migration surface 保持为只读；rebalance 的 mutation/control-plane API 仍有意不纳入本轮范围。
- Added compatibility verification confirming planned migration respects existing lease expiry and preserves cursor continuity during failover-style takeover.
- 已补充兼容性验证，确认 planned migration 会尊重既有 lease expiry，并在 failover-style takeover 期间保持 cursor continuity。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Ownership Closure Round 3)

### Delivered / 已交付

- Scheduler worker-owned partition assignment is now wired into startup/runtime status rather than remaining an internal-only runtime helper.
- scheduler worker-owned partition assignment 现已正式接入启动路径 / 运行态状态面，而不再只是内部 runtime helper。
- `index.ts` now resolves `schedulerWorkerId` + owned partitions at startup, and `/api/status` now exposes `scheduler: { worker_id, partition_count, owned_partition_ids }`.
- `index.ts` 现会在启动时解析 `schedulerWorkerId` 与 owned partitions，且 `/api/status` 现已暴露 `scheduler: { worker_id, partition_count, owned_partition_ids }`。
- Added startup/status-oriented validation so operator-facing runtime diagnostics can directly observe current scheduler ownership boundaries.
- 已补齐面向 startup/status 的验证，使 operator-facing runtime diagnostics 可直接观察当前 scheduler ownership 边界。

### Verification / 验证

- `pnpm --filter yidhras-server exec prisma generate`: pass
- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:scheduler-runtime-status`: pass
- `pnpm --filter yidhras-server test:scheduler-multi-worker-partitioning`: pass
- `pnpm --filter yidhras-server test:scheduler-failover`: pass
- `pnpm --filter yidhras-server smoke:startup`: pass

### Note / 说明

- Current closure round still exposes ownership via startup config and status snapshot only; dynamic runtime rebalance and control-plane mutation APIs are still not included.
- 当前第三轮收口仍仅通过启动配置与状态快照暴露 ownership；dynamic runtime rebalance 与 control-plane mutation API 仍未纳入。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Ownership / Failover Closure Round 2)

### Delivered / 已交付

- Scheduler worker ownership is now no longer limited to “all workers iterate all partitions”; runtime now supports explicit worker-owned partition selection.
- 调度器 worker ownership 现已不再局限于“所有 worker 遍历全部 partition”；runtime 现已支持显式 worker-owned partition 选择。
- Added partition assignment resolution via explicit `partitionIds` input and env-driven mapping: `SCHEDULER_WORKER_PARTITIONS` or `SCHEDULER_WORKER_TOTAL + SCHEDULER_WORKER_INDEX`.
- 已新增基于显式 `partitionIds` 输入和环境变量映射的 partition assignment 解析：`SCHEDULER_WORKER_PARTITIONS` 或 `SCHEDULER_WORKER_TOTAL + SCHEDULER_WORKER_INDEX`。
- Added dedicated failover verification to confirm lease expiry handoff and cursor continuity on a partition.
- 已新增 failover 专项验证，确认 partition 上的 lease 过期接管与 cursor 连续性。
- Added dedicated multi-worker partition ownership verification to confirm workers only execute assigned partitions.
- 已新增 multi-worker partition ownership 专项验证，确认 worker 只执行自己被分配的 partitions。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:scheduler-multi-worker-partitioning`: pass
- `pnpm --filter yidhras-server test:scheduler-failover`: pass

### Note / 说明

- Current closure round still keeps static assignment only; dynamic rebalance / automatic ownership migration remains future work.
- 当前第二轮收口仍只覆盖静态 assignment；dynamic rebalance / 自动 ownership 迁移仍属后续工作。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Partitioned Multi-Worker Baseline)

### Delivered / 已交付

- Scheduler deployment baseline is now partition-aware instead of relying on a single global lease/cursor pair.
- 调度器部署语义基线现已升级为 partition-aware，不再只依赖单一全局 lease/cursor。
- Added stable hash/bucket partition mapping, partition-scoped `SchedulerLease` / `SchedulerCursor`, and partition metadata on `SchedulerRun` / `SchedulerCandidateDecision`.
- 已新增稳定 hash/bucket partition 映射、partition-scoped `SchedulerLease` / `SchedulerCursor`，并在 `SchedulerRun` / `SchedulerCandidateDecision` 上补齐 partition 元信息。
- Runtime scheduler now executes per-partition runs, renews partition lease during execution, and exposes aggregated `scheduler_run_ids` / `partition_ids` in scheduler run results.
- runtime scheduler 现已按 partition 执行 run，在执行过程中续租 partition lease，并在调度结果中聚合暴露 `scheduler_run_ids` / `partition_ids`。
- Scheduler observability/query surfaces now expose `partition_id` filters and partition/worker-aware summary + trends + operator/agent projections.
- scheduler observability / query surface 现已补齐 `partition_id` 过滤能力，以及 partition/worker-aware 的 summary + trends + operator/agent projection。

### Verification / 验证

- `pnpm --filter yidhras-server exec prisma generate`: pass
- `pnpm --filter yidhras-server exec prisma migrate deploy`: pass
- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:scheduler-lease`: pass
- `pnpm --filter yidhras-server test:agent-scheduler-projection`: pass
- `pnpm --filter yidhras-server test:scheduler-crosslink-projection`: pass
- `pnpm --filter yidhras-server test:scheduler-run-level-aggregation`: pass
- `pnpm --filter yidhras-server test:scheduler-operator-projection`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass
- `pnpm --filter yidhras-server exec tsx src/e2e/scheduler_multi_worker_partitioning.ts`: pass
- `pnpm --filter yidhras-server test:scheduler-queries`: pass

### Note / 说明

- Current delivery is intentionally a baseline: workers still iterate all known partitions and compete via partition lease; dynamic rebalance / stronger ownership semantics remain follow-up work.
- 当前交付有意保持为 baseline：worker 当前仍是遍历已知 partition 并通过 partition lease 竞争，不包含 dynamic rebalance / 更强 ownership 语义。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Operator Projection Baseline)

### Delivered / 已交付

- Scheduler observability now exposes an operator-facing aggregation projection for overview-style consumption.
- scheduler observability 现已新增面向 overview/operator 的聚合 projection。
- Added operator projection composition over `latest_run / summary / trends / recent_runs / recent_decisions / highlights`.
- 已新增基于 `latest_run / summary / trends / recent_runs / recent_decisions / highlights` 的 operator projection 组合层。
- Operator highlights now further expose skipped/failure/workflow-state summary cues for the latest run.
- operator highlights 现已进一步补充最近一次 run 的 skipped/failure/workflow-state 摘要线索。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:scheduler-operator-projection`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Run-Level Cross-Link Aggregation)

### Delivered / 已交付

- Scheduler run read models now expose a lightweight run-level `cross_link_summary` for operator-facing aggregation.
- scheduler run 读模型现已新增轻量 run-level `cross_link_summary`，服务于 operator 级聚合总览。
- `cross_link_summary` 当前包含 `linked_workflow_count / workflow_state_breakdown / linked_intent_type_breakdown / status_breakdown / recent_audit_summaries`。
- `cross_link_summary` 当前包含 `linked_workflow_count / workflow_state_breakdown / linked_intent_type_breakdown / status_breakdown / recent_audit_summaries`。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:scheduler-run-level-aggregation`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Cross-Link Projection Phase 1)

### Delivered / 已交付

- Scheduler decision read paths now expose a minimal workflow/audit cross-link read model for created jobs.
- scheduler decision 读路径现已为 created jobs 暴露最小 workflow/audit cross-link read model。
- `listSchedulerDecisions()`、scheduler run read models、以及 agent scheduler projection timeline 现在统一带 `workflow_link`。
- `listSchedulerDecisions()`、scheduler run read model 与 agent scheduler projection timeline 现已统一带 `workflow_link`。
- `workflow_link` 现已进一步包含 `intent_type / dispatch_stage / failure_stage / failure_code / outcome_summary_excerpt / audit_entry.summary` 等解释型摘要字段。
- `workflow_link` 现已进一步包含 `intent_type / dispatch_stage / failure_stage / failure_code / outcome_summary_excerpt / audit_entry.summary` 等解释型摘要字段。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:scheduler-crosslink-projection`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Agent Scheduler Actor Projection)

### Delivered / 已交付

- Agent scheduler observability now exposes an actor-centric projection endpoint for operator-facing reads.
- agent scheduler 观测层现已新增 actor-centric projection 接口，用于 operator 视角读取。
- Added `GET /api/agent/:id/scheduler/projection` with actor summary, reason/skipped_reason breakdown, recent timeline, and recent run/job linkage.
- 已新增 `GET /api/agent/:id/scheduler/projection`，返回 actor summary、reason/skipped_reason breakdown、recent timeline 与 recent run/job linkage。
- Scheduler candidate decision read model now includes `scheduler_run_id`, so scheduler run linkage is directly available in timeline items.
- scheduler candidate decision 读模型现已包含 `scheduler_run_id`，timeline item 可直接表达 scheduler run linkage。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:agent-scheduler-projection`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Fine-Grained Replay-Aware Suppression)

### Delivered / 已交付

- Scheduler recovery-window suppression is now fine-grained and priority-aware.
- 调度器 recovery-window suppression 现已升级为细粒度、按优先级生效。
- Replay/retry windows now continue suppressing periodic cadence, while low-priority event-driven candidates can also be suppressed and high-priority `event_followup` can survive.
- replay / retry window 现继续 suppress periodic cadence，同时也可 suppress 低优先级 event-driven candidate，而高优先级 `event_followup` 可保留。
- Skip taxonomy is now expanded to `replay_window_periodic_suppressed` / `replay_window_event_suppressed` / `retry_window_periodic_suppressed` / `retry_window_event_suppressed`.
- skip taxonomy 现已扩展为 `replay_window_periodic_suppressed` / `replay_window_event_suppressed` / `retry_window_periodic_suppressed` / `retry_window_event_suppressed`。
- Scheduler summary/read-model paths now surface the fine-grained suppression reasons without changing the external envelope structure.
- scheduler summary/read-model 路径现已可暴露这些细粒度 suppression reason，同时未改变外层 transport envelope 结构。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass

### Note / 说明

- Current e2e keeps tolerant semantics for the high-priority survive branch because `pending_workflow` baseline may still block creation in non-deterministic runs.
- 当前 e2e 对高优先级 survive 分支保持容忍式语义校验，因为 `pending_workflow` 基线在非确定场景下仍可能阻断创建。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Frontend Graph + Mapping Increment)

### Delivered / 已交付

- Graph quick roots 已落地，支持从 projection 的 active roots 更快切换 root。
- Graph search context、inspector 分组与动作解释已增强，Graph 更接近 operator 定位 / 解释工作区。
- Timeline → Social 已改为优先使用 `source_action_intent_id + tick`，仅在缺少 intent 时回退到 keyword 语义上下文。
- Social → Timeline 已改为基于 tick 的 timeline slice 定位，不再使用 `post.id -> event_id` 的误导性假设。
- Social / Timeline 页面已新增 mapping context banner，用于明确当前链路是精确缩圈还是语义 / 上下文定位。
- Timeline tick 过滤比较已回归 string-first，未在前端 route/store/view-model 层重新引入 `BigInt`。

### Verification / 验证

- `pnpm --filter web typecheck`: pass
- `pnpm --filter web lint`: pass
- `pnpm --filter web test:unit`: pass

### Manual Verification Scope / 手动验证范围

- Graph：select node → focus selected → use selected as root → quick roots switch → graph → agent/workflow → clear filters / empty-result recovery
- Timeline：select event → open workflow → open social context → inspect intent-first / keyword-fallback hint → return to source
- Social：select post → open workflow → open timeline slice → inspect contextual mapping hint → return to source

### Note / 说明

- `nuxt typecheck` 仍会输出 `vue-router/volar/sfc-route-blocks` 对 `@vue/language-core` 的外部依赖 warning，但命令最终返回成功。
- 该 warning 当前继续视为工具链噪声，不视为本轮 Graph / Mapping 增量回归。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Replay-Aware Suppression baseline)

### Delivered / 已交付

- Scheduler candidate evaluation is now replay/retry recovery-window aware for periodic cadence suppression.
- 调度器 candidate evaluation 现已对 replay / retry recovery window 具备 periodic cadence suppression 语义。
- Added recovery-window helper based on `DecisionJob.intent_class`, plus new skip reasons: `replay_window_suppressed` / `retry_window_suppressed`.
- 已基于 `DecisionJob.intent_class` 增加 recovery-window helper，并新增 skip reason：`replay_window_suppressed` / `retry_window_suppressed`。
- Scheduler summary projection now includes these suppression reasons through top skipped reason aggregation.
- scheduler summary projection 现已可通过 top skipped reason 聚合体现 suppression 行为。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass
- `pnpm --filter yidhras-server test:workflow-replay`: pass
- `pnpm --filter yidhras-server test:scheduler-queries`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Summary / Trend projections)

### Delivered / 已交付

- Scheduler observability now exposes summary and trend projections on top of runs/decisions query APIs.
- 调度器观测层现已在 runs/decisions 查询 API 之上新增 summary 与 trend projection。
- Added backend helpers and routes for scheduler summary/trends, including top reasons, top skipped reasons, top actors, and intent_class breakdown.
- 已新增 scheduler summary/trends 的后端 helper 与路由，包含 top reasons、top skipped reasons、top actors 与 intent_class breakdown。
- Extended scheduler query e2e to cover summary/trends read paths.
- 已扩展 scheduler query e2e，覆盖 summary / trends 读路径。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:scheduler-queries`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Scheduler Replay-Aware Scheduling baseline)

### Delivered / 已交付

- `DecisionJob.intent_class` has been added as a stable workflow intent classification layer.
- 已新增 `DecisionJob.intent_class`，作为稳定的工作流意图分类层。
- direct submit / scheduler / replay / retry paths now write consistent `intent_class` and `job_source` metadata.
- direct submit / scheduler / replay / retry 路径现已写入一致的 `intent_class` 与 `job_source` 元数据。
- workflow job read paths and snapshots now expose `intent_class` for downstream observability.
- workflow job 读路径与快照现已暴露 `intent_class`，用于下游观测与统计。

### Verification / 验证

- `pnpm --filter yidhras-server exec prisma generate`: pass
- `pnpm --filter yidhras-server exec prisma migrate deploy`: pass
- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass
- `pnpm --filter yidhras-server test:workflow-replay`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Agent Scheduler Phase 4B baseline)

### Delivered / 已交付

- Frontend operator projection now starts consuming scheduler read APIs on Overview and Agent pages.
- 前端 operator projection 现已开始在 Overview 与 Agent 页面消费 scheduler 读接口。
- Overview now exposes recent scheduler runs and recent scheduler decisions cards for operator drill-down.
- Overview 现已暴露 recent scheduler runs 与 recent scheduler decisions 卡片，用于 operator drill-down。
- Agent detail now exposes a scheduler timeline card for recent scheduling decisions.
- Agent detail 页面现已补充 scheduler timeline 卡片，用于展示最近调度决策轨迹。

### Verification / 验证

- `pnpm --filter web lint`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Agent Scheduler Phase 4A)

### Delivered / 已交付

- Scheduler query surface is now expanded with filtered/paginated runs and decisions APIs.
- scheduler 查询面现已扩展为支持过滤/分页的 runs 与 decisions API。
- Added backend query helpers for scheduler runs/decisions with cursor, tick range, worker/actor, kind/reason/skipped_reason filters.
- 已新增 scheduler runs/decisions 的后端查询 helper，支持 cursor、tick range、worker/actor、kind/reason/skipped_reason 过滤。
- Added standalone e2e coverage for scheduler queries and aligned package scripts.
- 已新增 scheduler queries 独立 e2e 覆盖，并同步 package 脚本。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:scheduler-queries`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Agent Scheduler Phase 3C)

### Delivered / 已交付

- Scheduler multi-worker safety baseline is now landed with persisted `SchedulerLease` and `SchedulerCursor` models.
- scheduler multi-worker safety 基线现已落地，包含持久化的 `SchedulerLease` 与 `SchedulerCursor` 模型。
- Runtime scheduler now acquires a leader-style lease before scanning and updates a minimal cursor/watermark after successful runs.
- runtime scheduler 现会在扫描前获取 leader-style lease，并在成功执行后更新最小 cursor / watermark。
- Simulation loop now passes a dedicated `schedulerWorkerId`, and a standalone scheduler lease e2e script is now available.
- simulation loop 现已传入专用 `schedulerWorkerId`，并新增独立的 scheduler lease e2e 脚本。

### Verification / 验证

- `pnpm --filter yidhras-server exec prisma generate`: pass
- `pnpm --filter yidhras-server exec prisma migrate deploy`: pass
- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:scheduler-lease`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Agent Scheduler Phase 3B)

### Delivered / 已交付

- Scheduler observability read model baseline is now landed with persisted `SchedulerRun` and `SchedulerCandidateDecision` snapshots.
- scheduler observability read model 基线现已落地，包含持久化的 `SchedulerRun` 与 `SchedulerCandidateDecision` 快照。
- Runtime scheduler now records per-run summary plus per-candidate decision snapshots, including chosen reason, secondary reasons, priority score, skip reason, and created job linkage.
- runtime scheduler 现会记录每轮 summary 与每个 candidate 的决策快照，包含 chosen reason、secondary reasons、priority score、skip reason 与 created job linkage。
- Minimal scheduler read surface is now exposed via `/api/runtime/scheduler/runs/latest`, `/api/runtime/scheduler/runs/:id`, and `/api/agent/:id/scheduler`.
- 最小 scheduler 只读面现已通过 `/api/runtime/scheduler/runs/latest`、`/api/runtime/scheduler/runs/:id` 与 `/api/agent/:id/scheduler` 暴露。

### Verification / 验证

- `pnpm --filter yidhras-server exec prisma generate`: pass
- `pnpm --filter yidhras-server exec prisma migrate deploy`: pass
- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass

### Scope / 范围说明

- Current delivery provides the minimal read model and internal persistence baseline; richer query filters, operator-level visualization, and lease/cursor-based multi-worker safety remain future work.
- 当前交付提供的是最小读模型与内部持久化基线；更丰富的查询过滤、operator 级可视化与基于 lease/cursor 的 multi-worker safety 仍属后续工作。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Agent Scheduler Phase 2)

### Delivered / 已交付

- Agent Scheduler Phase 2 baseline landed with durable scheduled jobs and minimal event-driven cadence.
- Agent Scheduler 第二阶段基线已落地：包含 durable scheduled jobs 与最小 event-driven cadence。
- `DecisionJob` now carries `scheduled_for_tick`, and decision runner/claim logic now respects future-scheduled execution windows.
- `DecisionJob` 已支持 `scheduled_for_tick`，decision runner / claim 逻辑已正确尊重 future-scheduled execution window。
- Scheduler now combines `periodic` and `event_driven` candidates, and writes `scheduler_scheduled_for_tick` plus structured scheduler reason metadata into `request_input.attributes`.
- scheduler 现已合并 `periodic` 与 `event_driven` 候选，并将 `scheduler_scheduled_for_tick` 与结构化 scheduler reason 元信息写入 `request_input.attributes`。
- Minimal follow-up signals are now sourced from `Event`, `RelationshipAdjustmentLog`, and `SNRAdjustmentLog`.
- 最小 follow-up signal 现已从 `Event`、`RelationshipAdjustmentLog` 与 `SNRAdjustmentLog` 中提取。

### Verification / 验证

- `pnpm --filter yidhras-server exec prisma migrate deploy`: pass
- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass

### Scope / 范围说明

- Current delivery is still a baseline: event-driven cadence is intentionally minimal, and scheduler/operator read models plus multi-worker safety remain follow-up work.
- 当前交付仍为基线版本：event-driven cadence 有意保持最小实现，scheduler/operator read model 与 multi-worker safety 仍属后续项。

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录)

### Delivered / 已交付

- Backend Agent Scheduler v1 landed as a first-pass runtime scheduling baseline.
- 后端 Agent Scheduler v1 已落地，形成首轮 runtime 自动调度基线。
- Runtime loop now runs `expire bindings -> sim.step -> agent scheduler -> decision runner -> action dispatcher`.
- 当前 runtime loop 已形成 `expire bindings -> sim.step -> agent scheduler -> decision runner -> action dispatcher` 闭环顺序。
- Scheduler-created jobs now use `sch:` idempotency keys and write scheduler metadata into `DecisionJob.request_input.attributes`.
- scheduler 生成的 job 已统一使用 `sch:` 前缀幂等 key，并将调度元信息写入 `DecisionJob.request_input.attributes`。

### Verification / 验证

- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass

### Scope / 范围说明

- Current delivery is the minimal scheduler baseline only: active-agent scan, pending workflow dedupe, cooldown, runtime-loop integration, and e2e verification.
- 当前交付仅覆盖最小 scheduler 基线：active agent 扫描、pending workflow 去重、cooldown、runtime-loop 接入与 e2e 验证。

---

## Current Snapshot / 当前快照 (2026-03-30)

### Delivered / 已交付

- `apps/web` Operator UI polish 第一阶段已完成：统一页面骨架、来源上下文与回跳、Graph focus/root/result feedback、freshness 与轻量通知机制均已落地。
- `apps/web` 当前已有较明确的前端 Guardrails 与验收边界，主要体现在 `apps/web/README.md` 与相关前端文档中。
- Frontend Product Enhancement Phase 2 首批已完成：Workflow detail panel 强化、Social detail 增强、Timeline detail pane 落地，且未破坏第一阶段交互基线。
- 根文档、前端文档与前端增强计划文档已重新对齐到当前实现状态。

### Verification / 验证

- `pnpm --filter web typecheck`: pass
- `pnpm --filter web lint`: pass
- `pnpm --filter web test:unit`: pass

### Manual Verification Scope / 手动验证范围

- Workflow：list → select job → retry → related entity drill-down
- Social：select post → open agent / workflow / timeline
- Timeline：select event → detail pane → open workflow / social
- Graph：Agent / Workflow drill-down、focus、use selected as root、clear filters 回归通过

### Note / 说明

- `nuxt typecheck` 仍会输出 `vue-router/volar/sfc-route-blocks` 对 `@vue/language-core` 的外部依赖 warning，但命令最终返回成功。
- 该 warning 当前视为工具链噪声，不视为本轮 UI polish / product enhancement 回归。

---

## Current Snapshot / 当前快照 (2026-03-28)

### Commands / 命令

- `npm run lint --prefix apps/server`
- `npm run typecheck --prefix apps/server`
- `npm run lint --prefix apps/web`
- `npm run typecheck --prefix apps/web`
- `npm run smoke --prefix apps/server`

### Results / 结果

- `apps/server`: lint pass
- `apps/server`: typecheck pass
- `apps/server`: smoke (`startup` + `endpoints`) pass
- `apps/web`: lint pass
- `apps/web`: typecheck pass

### Conclusion / 结论

- No outstanding recorded lint debt currently exists.
- 当前没有待记录的 lint 债务。
- The repo is in a green verification state at the engineering baseline level.
- 当前仓库在工程基线层面处于绿色可验证状态。

---

## Historical Lint Baseline / 历史 Lint 基线 (2026-03-20)

### Initial Baseline / 初始基线

- `apps/server`: 0 errors, 29 warnings
- `apps/web`: 0 errors, 2 warnings

### Cleanup Outcome / 清理结果

- `apps/server`: 0 errors, 0 warnings
- `apps/web`: 0 errors, 0 warnings
- `apps/server`: typecheck pass
- `apps/web`: typecheck pass

### Hardened-as-Error Rules / 已提升为 Error 的规则

- Shared:
  - `@typescript-eslint/no-explicit-any`
  - `@typescript-eslint/no-unused-vars`
  - `simple-import-sort/imports`
  - `simple-import-sort/exports`
- Server-only:
  - `prefer-const`
  - `no-case-declarations`

### Note / 说明

- The old per-file warning breakdown and cleanup queue were intentionally removed during the 2026-03-28 slimming pass.
- 2026-03-28 的瘦身整理中，旧的逐文件 warning 统计与清理队列已移除。

---

## Runtime & Acceptance Notes / 运行时与验收边界 (2026-03-21)

### Accepted Constraints / 已冻结边界

- Degraded startup is acceptable for fresh clones when `health_level=degraded` and `runtime_ready=false`.
- 首次拉取仓库时，若 `health_level=degraded` 且 `runtime_ready=false`，视为允许启动。
- World-pack-gated endpoints must consistently return `503/WORLD_PACK_NOT_READY` when runtime is not ready.
- 受 world-pack 约束的接口在运行时未就绪时必须统一返回 `503/WORLD_PACK_NOT_READY`。
- `prepare:runtime` is the single startup preparation entry.
- `prepare:runtime` 是统一启动前置准备入口。
- Runtime speed policy follows `override > world_pack > default(1)`.
- 运行时速度策略遵循 `override > world_pack > default(1)`。

### Delivered Follow-Ups / 已落地跟进

- Runtime speed override API: `POST /api/runtime/speed`
- `runtime_speed.override_since` added to status/override snapshots
- Notification `details` enriched with runtime-speed metadata

---

## Major Verification Milestones / 关键验证里程碑

## 2026-03-22 —M0 Verification Snapshot / 工程基线验收

### Delivered / 已确认

- Repo-level lint/typecheck baseline is green for both apps.
- 前后端 lint/typecheck 基线均已打通并通过。
- Identity binding migration stack started landing.
- 身份绑定相关 migration 开始落地。

### Verification / 验证

- `npm run lint --prefix apps/server`: pass
- `npm run lint --prefix apps/web`: pass
- `npm run typecheck --prefix apps/server`: pass
- `npm run typecheck --prefix apps/web`: pass

## 2026-03-22 — Identity Lifecycle Baseline / 身份生命周期基线

### Delivered / 已交付

- `IdentityNodeBinding` migration landed
- bind/query/unbind/expire APIs landed
- uniqueness guard for `identity_id + role + active`
- runtime auto-expire integrated into simulation loop
- atmosphere node seed (`atm-001`) and query endpoint landed

### Verification / 验证

- `npm run seed:identity --prefix apps/server`: pass
- `npm run lint --prefix apps/server`: pass
- `npm run typecheck --prefix apps/server`: pass

## 2026-03-23 — Backend Refactor + Phase B Stable Baseline

### Delivered / 已交付

- `apps/server/src/index.ts` became a composition root
- `app/create_app.ts` + `app/routes/*.ts` + `app/services/*.ts` boundaries landed
- request-id propagation and unified error envelope preserved
- inference Phase B baseline landed (`preview` / `run`, context/prompt/decision contracts, pluggable providers/sink)

### Verification / 验证

- `npm run lint --prefix apps/server`: pass
- `npm run typecheck --prefix apps/server`: pass
- `npm run smoke:startup --prefix apps/server`: pass
- `npm run smoke:endpoints --prefix apps/server`: pass

## 2026-03-24 — Phase D / Dispatcher / L4 / Memory / Prompt Pipeline Baseline

### Delivered / 已交付

- Prisma workflow models landed: `InferenceTrace`, `ActionIntent`, `DecisionJob`
- idempotency replay + failed-job retry landed
- loop-driven decision runner landed
- first-pass `post_message` dispatcher landed
- minimal L4 fields/policy/drop semantics landed
- aggregate workflow reads + failure-stage observation landed
- Memory Core v1 baseline landed
- Prompt Pipeline Phase 2 baseline landed (`policy-filter`, `memory-summary`, `token-budget-trimmer`)

### Verification / 验证

- `npm exec prisma migrate deploy --prefix apps/server`: pass
- `npm run lint --prefix apps/server`: pass
- `npm run typecheck --prefix apps/server`: pass
- `npm run smoke:startup --prefix apps/server`: pass
- `npm run smoke:endpoints --prefix apps/server`: pass

## 2026-03-27 — Root Docs Sync / 根文档对齐

### Delivered / 已交付

- `TODO.md`, `LOGIC.md`, `ARCH.md`, `README.md`, `API.md` were re-aligned to the actual codebase state.
- `TODO.md`、`LOGIC.md`、`ARCH.md`、`README.md`、`API.md` 已按真实代码状态重新对齐。

## 2026-03-28 — Full Repo Documentation Consistency Review / 全仓文档一致性复查

### Delivered / 已交付

- User-facing docs re-reviewed against real routes, runtime behavior, and current frontend state.
- 再次对照真实路由、运行时行为与前端现状，复查用户可见文档。
- `apps/web/README.md` was rewritten from the Nuxt starter template into a repo-specific frontend status document.
- `apps/web/README.md` 已从 Nuxt 默认模板改写为仓库专用前端状态说明。
- `草稿.md` and `.limcode/design|plans` docs were labeled as draft/historical artifacts rather than live status ledgers.
- `草稿.md` 与 `.limcode/design|plans` 文档已明确标记为草稿/历史规划产物，而非当前状态台账。
- `记录.md` was slimmed into a verification-focused record.
- `记录.md` 已重构为以验证证据为核心的精简记录。

### Verification / 验证

- `npm run lint --prefix apps/server`: pass
- `npm run typecheck --prefix apps/server`: pass
- `npm run lint --prefix apps/web`: pass
- `npm run typecheck --prefix apps/web`: pass
- `npm run smoke --prefix apps/server`: pass

## 2026-04 / 具体日期未补录 — pnpm Workspace Migration Verification Snapshot / pnpm Workspace 迁移验证快照

### Delivered / 已交付

- Root `pnpm workspace` baseline landed with root `package.json`, `pnpm-workspace.yaml`, and root `pnpm-lock.yaml`.
- 已完成根级 `pnpm workspace` 基线：新增根 `package.json`、`pnpm-workspace.yaml` 与根 `pnpm-lock.yaml`。
- Startup scripts and GitHub Actions were migrated from npm to pnpm.
- 启动脚本与 GitHub Actions 已从 npm 切换到 pnpm。
- Root/app docs were updated to use pnpm commands as the primary path.
- 根目录与 app 文档中的主命令路径已切换为 pnpm。
- App-level `package-lock.json` files were removed.
- 已移除 app 级 `package-lock.json`。

### Verification / 验证

- `pnpm install`: pass
- `pnpm lint`: pass
- `pnpm --filter yidhras-server exec prisma generate`: pass (manual rerun after migration)

### Known Follow-up / 后续待处理

- `pnpm typecheck` currently exposes pre-existing TypeScript issues in server code; these are recorded as follow-up debt and are not treated as pnpm-migration-specific regressions.
- 当前 `pnpm typecheck` 暴露出 server 侧既有 TypeScript 存量问题；已记录为后续债务处理，不视为 pnpm 迁移特有回归。

## 2026-04 / 具体日期未补录 — Contracts + Zod First-Batch Baseline / 契约与 Zod 第一批基线

### Delivered / 已交付

- `packages/contracts` pure contract package is now introduced with shared envelope, BigInt-string, and first-batch route schemas.
- 已引入 `packages/contracts` 纯契约包，包含共享 envelope、BigInt-string 与第一批路由 schema。
- Server-side boundary parsing helper is now introduced via `apps/server/src/app/http/zod.ts`, and `zod-validation-error` is now used for request-side validation failures.
- 后端已引入 `apps/server/src/app/http/zod.ts` 边界解析 helper，并开始使用 `zod-validation-error` 处理请求侧校验失败。
- First-batch route adoption is now landed for `system / clock / social`.
- 第一批路由接入已落地：`system / clock / social`。
- Frontend now has minimal contract/client integration through the clock path and no longer assumes raw `/api/clock` payloads.
- 前端已通过 clock 路径接入最小 contract/client 基线，不再假设 `/api/clock` 返回 raw payload。

### Verification / 验证

- `pnpm install`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter web lint`: pass
- `pnpm --filter yidhras-server smoke:startup`: pass

## 2026-04 / 具体日期未补录 — Contracts + Zod Second-Batch Route Adoption / 契约与 Zod 第二批路由接入

### Delivered / 已交付

- Shared identity/policy request schemas were added to `packages/contracts`.
- 已在 `packages/contracts` 中补充 identity/policy 请求侧共享 schema。
- Server-side request boundary adoption is now landed for `apps/server/src/app/routes/identity.ts` and `policy.ts`.
- 后端请求边界接入已扩展到 `apps/server/src/app/routes/identity.ts` 与 `policy.ts`。
- The current route-level migration continues to keep business rules in service modules rather than pushing them into Zod schemas.
- 当前路由层迁移继续保持“业务规则留在 service，不塞进 Zod schema”的边界。

### Verification / 验证

- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter web lint`: pass
- `pnpm --filter yidhras-server smoke:startup`: pass

## 2026-04 / 具体日期未补录 — Contracts + Zod Third-Batch Route Adoption / 契约与 Zod 第三批路由接入

### Delivered / 已交付

- Shared inference/audit/graph request schemas were added to `packages/contracts`.
- 已在 `packages/contracts` 中补充 inference / audit / graph 的请求侧共享 schema。
- Server-side request boundary adoption is now landed for `apps/server/src/app/routes/inference.ts`, `audit.ts`, and `graph.ts`.
- 后端请求边界接入已扩展到 `apps/server/src/app/routes/inference.ts`、`audit.ts` 与 `graph.ts`。
- The third-batch route migration continues to use shared contract schemas while leaving business/state rules inside service modules.
- 第三批路由迁移继续采用共享 contract schema，同时保持业务规则与状态规则留在 service 层。

### Verification / 验证

- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter web lint`: pass
- `pnpm --filter yidhras-server smoke:startup`: pass

## 2026-04 / 具体日期未补录 — Docs Migration & Principle Solidification / docs 迁移与原则固化

### Delivered / 已交付

- `docs/` directory is now introduced for detailed architecture/API/logic documentation.
- 已建立 `docs/` 目录承载详细架构/API/逻辑文档。
- `API.md`, `ARCH.md`, and `LOGIC.md` were migrated into `docs/` as the canonical detailed docs.
- `API.md`、`ARCH.md`、`LOGIC.md` 已迁移至 `docs/` 并作为详细文档的正式位置。
- Root docs were reshaped so that `README.md`, `AGENTS.md`, and `TODO.md` remain entry-oriented while detailed references point into `docs/`.
- 根目录文档已重整为入口导向：`README.md`、`AGENTS.md`、`TODO.md` 继续保留，详细内容统一指向 `docs/`。
- Contract/validation principles and the BigInt-over-HTTP string rule were formally written into `docs/ARCH.md` and `docs/LOGIC.md`.
- 已将 contract/validation 原则与 BigInt-over-HTTP string 规则正式写入 `docs/ARCH.md` 与 `docs/LOGIC.md`。

### Verification / 验证

- `docs/` exists: pass
- Root entry docs preserved: pass
- Core cross-links updated to `docs/*`: pass

### Known Follow-up / 后续待处理

- `apps/web/components/L2Graph.vue` still has a Nuxt/Vue/Cytoscape DOM container typing incompatibility under `nuxt typecheck`.
- `apps/web/components/L2Graph.vue` 在 `nuxt typecheck` 下仍存在 Nuxt/Vue/Cytoscape 的 DOM container 类型兼容问题。
- This is explicitly treated as a frontend-owner follow-up item and is not being force-fit into the current backend/contract migration stream.
- 该问题已明确作为前端负责人后续处理项，不再强行混入当前后端/contract 迁移主线。

---

## Verification Commands / 常用验证命令

- `pnpm --filter yidhras-server lint`
- `pnpm --filter yidhras-server typecheck`
- `pnpm --filter yidhras-server smoke`
- `pnpm --filter web lint`
- `pnpm --filter web typecheck`

---

## Maintenance Rules / 维护规则

- Keep this file evidence-oriented and maintainable.
- 本文件保持证据导向，并控制维护成本。
- Put current status in `TODO.md`。
- 当前状态写入 `TODO.md`。
- Put detailed explanations in `docs/`。
- 详细说明写入 `docs/`。
- Append new dated snapshots instead of restoring long-running diary sections.
- 如需追加内容，请新增简短日期快照，而不是恢复超长流水账。
