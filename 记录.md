# 记录

- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server test:scheduler-operator-projection`: pass
- `pnpm --filter yidhras-server test:scheduler-queries`: pass
- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:agent-scheduler`: pass
- `pnpm --filter yidhras-server test:workflow-replay`: pass
- `SMOKE_PORT=3102 pnpm --filter yidhras-server test:workflow-retry-semantics`: pass
- `pnpm --filter yidhras-server test:workflow-replay-scheduler-suppression`: pass
- `pnpm --filter web exec vue-tsc --noEmit`: pass
- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:world-pack-scenario-demo`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, World-Pack Scenario-Driven Death Note Demo Baseline)
### Delivered / 已交付

- World-pack contract is no longer limited to `variables/prompts/simulation_time`; `death_note` now also declares `scenario / event_templates / actions / decision_rules`.
- world-pack contract 不再只覆盖 `variables/prompts/simulation_time`；`death_note` 现已扩展声明 `scenario / event_templates / actions / decision_rules`。
- Added `ScenarioEntityState` as the minimal runtime carrier for pack-driven actor/artifact/world state.
- 已新增 `ScenarioEntityState`，作为 pack-driven actor / artifact / world state 的最小运行时承载层。
- `SimulationManager.init()` now materializes scenario agents / identities / bindings / relationships / artifact/world state from the active world-pack.
- `SimulationManager.init()` 现会在启动时把 active world-pack 中的 scenario agents / identities / bindings / relationships / artifact/world state materialize 到数据库。
- Inference context and prompt assembly now expose `pack_state` and `pack_runtime`, allowing rule-based inference to react to pack-owned artifacts and actor state.
- inference context 与 prompt 组装现已暴露 `pack_state` 与 `pack_runtime`，使 rule-based inference 能基于 pack-owned artifacts 与 actor state 做反应。
- Rule-based inference now evaluates pack decision rules first, then falls back to the previous generic behavior.
- rule-based inference 现会优先命中 pack decision rules，再回退到既有通用行为。
- Action dispatcher now supports pack action registry plus `claim_artifact / set_actor_state / emit_event` executors; pack event templates can write formal narrative events into timeline.
- action dispatcher 现已支持 pack action registry，以及 `claim_artifact / set_actor_state / emit_event` executors；pack event templates 现在可把正式 narrative events 写入 timeline。
- Added focused proof `apps/server/src/e2e/world_pack_scenario_demo.ts`, covering the current baseline flow: `notebook_discovered -> claim_death_note -> murderous_intent_formed -> timeline/scheduler evidence`.
- 已新增 focused proof `apps/server/src/e2e/world_pack_scenario_demo.ts`，覆盖当前 baseline flow：`notebook_discovered -> claim_death_note -> murderous_intent_formed -> timeline/scheduler evidence`。
- E2E startup helper now supports runtime preflight preparation before spawning `serve:e2e`, which avoids having to manually run migration/seed steps for this scenario proof.
- e2e 启动 helper 现已支持在拉起 `serve:e2e` 前执行 runtime preflight preparation，从而避免该场景 proof 需要手工先跑 migration/seed。

### Verification / 验证

- `pnpm --filter yidhras-server lint`: pass
- `pnpm --filter yidhras-server typecheck`: pass
- `pnpm --filter yidhras-server test:world-pack-scenario-demo`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Replay -> Scheduler Suppression Closure)
### Delivered / 已交付

- Added a focused replay-to-scheduler linkage e2e at `apps/server/src/e2e/workflow_replay_scheduler_suppression.ts`.
- 已新增 `apps/server/src/e2e/workflow_replay_scheduler_suppression.ts`，专门验证 replay API 提交后的 scheduler suppression 联动证据。
- The new test submits a base workflow, triggers `/api/inference/jobs/:id/replay`, then polls scheduler public read APIs to confirm `replay_window_periodic_suppressed` appears for the replayed actor.
- 新用例会先提交基础 workflow，再触发 `/api/inference/jobs/:id/replay`，随后轮询 scheduler 公共读接口，确认被 replay 的 actor 能观测到 `replay_window_periodic_suppressed`。
- No runtime code change was required for this closure pass; the existing fine-grained suppression baseline already satisfied the replay linkage expectation.
- 本轮收尾未触发新的 runtime 修复；现有 fine-grained suppression baseline 已能满足 replay -> scheduler linkage 的验证预期。

### Verification / 验证

- `pnpm --filter yidhras-server test:workflow-replay-scheduler-suppression`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Agent Scheduler Phase 4B Operator Workspace)

### Delivered / 已交付

- Scheduler operator-facing observability is now promoted from overview-only cards to a dedicated Scheduler Workspace in the web console.
- scheduler 面向 operator 的观测面现已从 Overview 卡片升级为独立 Scheduler Workspace。
- Overview now directly consumes `/api/runtime/scheduler/operator` instead of stitching runs/decisions/summary/trends manually.
- Overview 现已直接消费 `/api/runtime/scheduler/operator`，不再手工拼接 runs/decisions/summary/trends。
- Agent detail now directly consumes `/api/agent/:id/scheduler/projection`, reusing server-side summary, breakdown, timeline, and linkage.
- Agent detail 页面现已直接消费 `/api/agent/:id/scheduler/projection`，复用服务端 summary / breakdown / timeline / linkage。
- Added `pages/scheduler.vue` plus `features/scheduler/*`, exposing highlights, summary/trends, recent runs, recent decisions, ownership, workers, migrations, and rebalance views.
- 已新增 `pages/scheduler.vue` 与 `features/scheduler/*`，补齐 highlights、summary/trends、recent runs / decisions、ownership、workers、migrations 与 rebalance 视图。
- Shell activity rail and source-context model now formally include `scheduler` workspace and scheduler-specific recent targets.
- Shell activity rail 与 source-context 现已正式纳入 `scheduler` workspace 及 scheduler-specific recent targets。
- Scheduler drill-down baseline now supports run detail, decision -> workflow/agent, partition filter, and worker filter pivots.
- Scheduler drill-down 基线现已支持 run detail、decision -> workflow/agent、partition filter 与 worker filter 跳转。
- Web scheduler API client/types are now aligned with operator/agent projection, ownership, workers, rebalance, richer run fields, and richer decision fields.
- 前端 scheduler API client / 类型现已对齐 operator/agent projection、ownership、workers、rebalance 以及更丰富的 run/decision 字段。

### Verification / 验证

- `pnpm --filter web test:unit`: pass
- `pnpm --filter web typecheck`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Agent Scheduler Phase 4B baseline)

### Delivered / 已交付

- Frontend operator projection now starts consuming scheduler read APIs on Overview and Agent pages.
- 前端 operator projection 现已开始在 Overview 与 Agent 页面消费 scheduler 读接口。
- Overview now exposes recent scheduler runs and recent scheduler decisions cards for operator drill-down.
- Overview 现已暴露 recent scheduler runs 与 recent scheduler decisions 卡片，用于 operator drill-down。
- Agent detail now exposes a scheduler timeline card for recent scheduling decisions.
- Agent detail 页面现已补充 scheduler timeline 卡片，用于展示最近调度决策轨迹。

### Verification / 验证

- `pnpm --filter web lint`: pass

---

## Current Snapshot / 当前快照 (2026-04 / 具体日期未补录, Agent Scheduler Phase 4A)

### Delivered / 已交付

- Scheduler query surface is now expanded with filtered/paginated runs and decisions APIs.
- scheduler query surface 现已扩展为支持过滤 / 分页的 runs 与 decisions APIs。
- Added `GET /api/runtime/scheduler/runs` and `GET /api/runtime/scheduler/decisions`, keeping envelope / BigInt transport stable.
- 已新增 `GET /api/runtime/scheduler/runs` 与 `GET /api/runtime/scheduler/decisions`，并保持 envelope / BigInt transport 稳定。
- Scheduler query surfaces now expose `partition_id` filters and partition/worker-aware summary + trends + operator/agent projections.
- scheduler query surface 现已补齐 `partition_id` 过滤能力，以及 partition/worker-aware 的 summary + trends + operator/agent projection。

### Verification / 验证

- `pnpm --filter yidhras-server test:scheduler-queries`: pass
- `pnpm --filter yidhras-server test:scheduler-operator-projection`: pass
- `pnpm --filter yidhras-server test:scheduler-run-level-aggregation`: pass
- `pnpm --filter yidhras-server typecheck`: pass

---

## Earlier Notes / 更早快照

- 其余历史记录保持不变，详见 git 历史或更早版本文档。