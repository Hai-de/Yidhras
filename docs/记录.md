## Runtime Boundary Notes / 当前记录（Ownership Matrix Audit In Progress）

### 已完成：SimulationManager 第一轮调整

#### 结构调整

- 新增 `apps/server/src/core/runtime_activation.ts`
  - pack activation/bootstrap 细节从 `SimulationManager.init()` 中抽离
- `apps/server/src/core/simulation.ts` 已收敛为 runtime facade / shell
  - `init()` 委托 activation
  - 新增 facade：
    - `resolvePackVariables()`
    - `getCurrentTick()`
    - `getAllTimes()`
    - `listAvailablePacks()`
    - `getPacksDir()`
- 生产代码中：
  - `context.sim.prisma` 已清零
  - `context.sim.resolver` 已清零
  - tick/calendar 访问已优先经由 facade

#### 验证

- `pnpm --filter yidhras-server typecheck` ✅
- `pnpm --filter yidhras-server test:unit -- --runInBand` ✅
- `pnpm --filter yidhras-server test:integration -- --runInBand` ✅

---

## 当前进行中：Ownership Matrix 审计

### 审计对象与当前观察

#### 1. `Event`

**当前宿主：** kernel-side Prisma  
**主要使用面：**

- `domain/rule/enforcement_engine.ts`
  - objective enforcement 在 `emit_events` 时直接写 `Prisma Event`
- `packs/runtime/projections/narrative_projection_service.ts`
  - narrative timeline 从 `context.prisma.event.findMany(...)` 读取事件
- `app/services/audit.ts`
  - unified audit feed 使用 `Event` 作为审计项
- `app/services/inference_workflow/repository.ts`
  - 作为 workflow follow-up signal 的输入来源
- `memory/short_term_adapter.ts`
  - 作为短期记忆输入之一

**初步结论：** `bridge-between-both`  
**理由：**

- 事件的产生源头可以来自 pack-governance objective rule
- 但事件的消费面明显跨越：audit / memory / workflow follow-up / narrative projection
- 它不是纯 pack 内部 runtime 状态，而是跨 pack-governance 与 kernel observability 的共享证据对象

**建议：**

- 短期继续保留在 kernel-side Prisma
- 在 design / ARCH / API 中明确其角色：
  - **kernel-hosted shared evidence/event ledger**
  - 而不是 pack-owned narrative source of truth
- 若未来引入 `PackOutboxEvent`，也应把 `Event` 视为投影/证据层宿主，而非直接迁入 pack runtime

---

#### 2. `Post`

**当前宿主：** kernel-side Prisma  
**主要使用面：**

- `app/services/social.ts`
  - 社交流主数据与写入宿主
- `app/services/audit.ts`
  - post 被纳入统一 audit feed
- `memory/short_term_adapter.ts`
  - 作为短期记忆输入之一

**初步结论：** `retain-in-kernel`  
**理由：**

- `Post` 本质上属于 social layer / public communication surface
- 它并不是 world governance core 的内生状态对象
- 它天然需要统一的身份、权限、审核、审计与 feed 查询能力
- 迁入 pack runtime 会削弱 social layer 的统一宿主意义

**建议：**

- 明确长期保留在 kernel-side Prisma
- 在 ownership matrix 中将其标记为：
  - **social/public communication kernel object**
- pack-governance 若影响社交流，只通过 invocation / audit / event / feed side-effect 间接体现，不将 `Post` pack-owned 化

---

#### 3. `ActionIntent`

**当前宿主：** kernel-side Prisma  
**主要使用面：**

- `app/services/action_dispatcher.ts`
  - 调度、claim、dispatch、drop、status 更新主宿主
- `domain/invocation/invocation_dispatcher.ts`
  - 将 `ActionIntent` bridge 为 `InvocationRequest`
- `app/services/audit.ts`
  - workflow 视图与意图轨迹
- `app/services/relational/graph_projection.ts`
  - graph relay/container 结构的一部分
- `memory/short_term_adapter.ts`
  - 短期记忆输入
- `inference/sinks/prisma.ts`
  - inference 输出持久化为 action intent

**初步结论：** `retain-in-kernel`  
**理由：**

- `ActionIntent` 是 workflow dispatch / reliability / retry / transmission policy 的核心对象
- 它是 subject-side / workflow-side 结构，而不是 world objective state 本身
- 当前 pack-governance 只是消费 `ActionIntent` 并桥接为 `InvocationRequest`
- 因此它更像 kernel workflow envelope，而不是 pack runtime object

**建议：**

- 长期保留在 kernel-side Prisma
- 明确其角色为：
  - **workflow / dispatch envelope**
  - 与 pack-governance 的关系是 bridge，而非宿主
- 后续若要进一步纯化，应强化 “ActionIntent -> InvocationRequest” 边界，而不是把 `ActionIntent` 迁入 pack runtime

---

#### 4. `InferenceTrace`

**当前宿主：** kernel-side Prisma  
**主要使用面：**

- `inference/sinks/prisma.ts`
  - provider / prompt / context / decision trace 的持久化宿主
- `app/services/inference_workflow/repository.ts`
  - workflow 联查
- `app/services/audit.ts`
  - workflow/audit 证据
- `memory/short_term_adapter.ts`
  - 短期记忆输入

**初步结论：** `retain-in-kernel`  
**理由：**

- `InferenceTrace` 是推理调试、provider observability、workflow replay 的内核证据对象
- 它不属于 pack runtime 世界状态
- 它天然依赖统一 inference service / provider / prompt pipeline

**建议：**

- 长期保留在 kernel-side Prisma
- 明确其角色为：
  - **inference observability / replay evidence**
- 不将其作为 pack-owned runtime model 推进

---

#### 5. `DecisionJob`

**当前宿主：** kernel-side Prisma  
**主要使用面：**

- `app/services/inference_workflow/repository.ts`
  - create / claim / retry / replay / state update 主宿主
- `inference/sinks/prisma.ts`
  - inference run 与 decision job 结果联动
- `app/services/audit.ts`
  - workflow 轨迹
- `memory/short_term_adapter.ts`
  - 短期记忆输入
- scheduler / operator observability

**初步结论：** `retain-in-kernel`  
**理由：**

- `DecisionJob` 本质上是 workflow scheduler / retry / replay / operator 管理对象
- 它不是 pack-governance core state
- 它天然服务于系统级任务执行与调度

**建议：**

- 长期保留在 kernel-side Prisma
- ownership matrix 中明确其角色为：
  - **workflow control-plane object**

---

#### 6. relationship runtime evidence

**当前宿主：** kernel-side Prisma  
**当前对象：**

- `Relationship`
- `RelationshipAdjustmentLog`

**主要使用面：**

- `app/services/action_dispatcher.ts`
  - relationship change side effects
- `app/services/relational/graph_projection.ts`
  - relational graph 主数据
- `app/services/relational/queries.ts`
  - relationship adjustment logs 查询
- `app/services/audit.ts`
  - 统一审计

**初步结论：** `retain-in-kernel`  
**理由：**

- relationship graph 更像 social/relational layer 的统一运行结构
- 它并不只服务单一 pack-governance 规则
- 图查询、关系调整、审计、操作台视图都依赖其统一宿主

**建议：**

- 继续留在 kernel-side Prisma
- 在文档中把它归为：
  - **relational/social evidence layer**
  - 而非 pack-governance runtime core

---

## Ownership Matrix 初步决策表

| 对象 | 当前宿主 | 初步结论 | 理由摘要 |
|---|---|---|---|
| `Event` | kernel Prisma | `bridge-between-both` | 由 pack objective rule 产生，但被 audit / memory / workflow / narrative 统一消费 |
| `Post` | kernel Prisma | `retain-in-kernel` | social/public communication 统一宿主 |
| `ActionIntent` | kernel Prisma | `retain-in-kernel` | workflow/dispatch envelope，通过 bridge 接入 pack-governance |
| `InferenceTrace` | kernel Prisma | `retain-in-kernel` | inference observability / replay evidence |
| `DecisionJob` | kernel Prisma | `retain-in-kernel` | workflow control-plane object |
| `Relationship` / `RelationshipAdjustmentLog` | kernel Prisma | `retain-in-kernel` | relational/social evidence 统一宿主 |

---

## 对 `PackOutboxEvent` 的判断

**当前代码中尚无正式 `PackOutboxEvent` 模型。**

### 初步结论

- **本轮不引入 `PackOutboxEvent` 实现**
- 但应在文档中明确：
  - 当前 pack-governance 与 kernel evidence 的桥，主要通过：
    - objective enforcement 直接写 `Event`
    - projection extraction / audit aggregation
    - workflow bridge

### 不立即引入的理由

1. 当前 bridge 已足够支撑 narrative / audit / operator / memory / workflow follow-up
2. 现阶段更重要的是先把 ownership matrix 说清楚
3. 若过早引入 `PackOutboxEvent`，容易把“边界澄清问题”提前升级成“事件基础设施重构问题”

### 后续触发条件

若未来出现以下任一需求，可重新考虑：

- pack runtime 需要更严格的异步出站事件边界
- 多 pack / 多 runtime instance 需要稳定的 outbox/extractor contract
- narrative/event audit 需要更强的幂等同步与重放机制

---

## 结论

ownership matrix 审计显示：

- **world governance core** 继续 pack-owned
- **workflow / social / audit / relational evidence** 大部分应继续保留 kernel-side
- 真正跨边界的核心对象主要是：
  - `Event`
  - 以及 `ActionIntent -> InvocationRequest` 这条 bridge

下一步建议：

1. 把上述决策同步到 design / ARCH / API
2. 固化 `/api/access-policy/*` 作为独立 access-policy 子系统的位置，并继续保持 `/api/entities/:id/overview` 为唯一 canonical entity overview
3. 再处理 compat API 的剩余长期调整

---

## Death Note Intent Grounder / 第一阶段完成记录

### 已完成实现

- 扩展 `world-death-note` pack 的第一版题材动作链：
  - notebook acquisition
  - rule learning
  - murderous intent formation
  - target intel gathering
  - target selection
  - judgement execution
  - investigation follow-up
  - case intel sharing / publish-style communication fallback
- 三份 Death Note pack 文件已保持同步：
  - `apps/server/templates/world-pack/death_note.yaml`
  - `data/configw/templates/world-pack/death_note.yaml`
  - `data/world_packs/death_note/config.yaml`
- 新增并接入 server-side Intent Grounder：
  - `apps/server/src/domain/invocation/intent_grounder.ts`
- inference runtime 已消费 active-pack `rules.invocation`
- `semantic_intent` / `intent_grounding` / `semantic_outcome` / `objective_effect_applied` 已进入 trace/workflow/audit evidence
- narrativized failure 已通过 `trigger_event` + `history` 落地，并在 timeline / audit / entity overview 中可见
- scheduler 已消费事件桥接 metadata：
  - `followup_actor_ids`
  - `impact_data.semantic_intent.target_ref`
- 修复了 direct kernel actions 在 Grounder 中的直通覆盖缺口：
  - `adjust_relationship`
  - `adjust_snr`

### 约束保持成立

- 未引入 Death Note 专用 governor/admin agent
- 未改变 public inference strategy surface，仍为：
  - `mock`
  - `rule_based`
- unexpected semantic action 不要求被预枚举；可 translated / narrativized，而不是一律 blocked

### 验证结果

- `pnpm --filter yidhras-server typecheck` ✅
- `pnpm --filter yidhras-server test:unit` ✅
- `pnpm --filter yidhras-server test:integration -- --runInBand` ✅
- `pnpm --filter yidhras-server test:e2e -- --runInBand` ✅

### 当前结论

Death Note Intent Grounder 第一阶段已完成：

- semantic intent → invocation grounding → capability execution → objective mutation / narrativized fallback 的闭环已形成
- investigation / collaboration follow-up 已具备最小 event-driven 回流
- server 侧验证已全部通过

---

## Agent Context Module MVP / 当前完成记录

### 已完成实现

- 新增 `apps/server/src/context/**` 模块边界：
  - `types.ts`
  - `service.ts`
  - `source_registry.ts`
  - `compat.ts`
  - `sources/legacy_memory.ts`
  - `sources/runtime_state.ts`
  - `workflow/orchestrator.ts`
- 已引入统一 `ContextNode / ContextRun / ContextRunDiagnostics` 基础模型
- inference context 已先通过 `ContextService.buildContextRun(...)` 构建上下文，再向下派生 legacy `memory_context`
- 当前 `memory_context` 已保留为 compatibility surface，用于兼容既有 memory/prompt processors
- prompt 主线已通过线性 `Context Orchestrator Lite` 收口既有处理阶段：
  - `memory_injection`
  - `policy_filter`
  - `summary_compaction`
  - `token_budget_trim`
- `InferenceTrace.context_snapshot` 已新增上下文诊断结构：
  - `context_module`
  - `context_debug`
  - `prompt_assembly`
  - `selected_node_summaries`
  - `dropped_nodes`

### 验证结果

- `pnpm --filter yidhras-server test:unit -- tests/unit/context_module.spec.ts tests/unit/context_orchestrator.spec.ts tests/unit/authority_perception_context.spec.ts` ✅
- `pnpm --filter yidhras-server test:integration -- tests/integration/inference-workflow-core.spec.ts` ✅
- `pnpm --filter yidhras-server test:e2e -- tests/e2e/smoke-endpoints.spec.ts tests/e2e/agent-overview.spec.ts` ✅

### 当前结论

- 当前已完成的是 **Context Module MVP**，不是通用 Prompt Workflow Engine
- 当前 orchestrator 仍是固定线性阶段，而非可视化/可配置 DAG
- 当前 `memory_context` 仍然存在，但其角色已是 compatibility surface，而不是新的 canonical upstream abstraction

---

## Context Module Policy / Overlay / Directive 深化记录

### 已完成实现

- 已引入 `Context Policy Engine` 最小版：
  - `apps/server/src/context/policy_engine.ts`
- policy 已从 fragment-level 进一步上移到 node-level / working-set：
  - `deny` 节点不会进入最终 working set
  - `hidden_mandatory` 节点也不会进入最终 working set，但会保留 hidden/denial trace 语义
  - `policy_filter` 当前仅保留 compatibility / fallback 角色
- 已引入 kernel-side overlay 子系统：
  - `apps/server/src/context/overlay/types.ts`
  - `apps/server/src/context/overlay/store.ts`
  - `apps/server/src/context/sources/overlay.ts`
  - `apps/server/prisma/schema.prisma` 中新增 `ContextOverlayEntry`
- overlay 当前通过 source adapter 重新 materialize 为：
  - `ContextNode(source_kind='overlay', visibility.level='writable_overlay')`
- `InferenceTrace.context_snapshot` 已新增可观测字段：
  - `policy_decisions`
  - `blocked_nodes`
  - `locked_nodes`
  - `visibility_denials`
  - `overlay_nodes_loaded`
  - `overlay_nodes_mutated`
- `/api/entities/:id/overview` 当前已新增：
  - `context_governance.latest_policy`
  - `context_governance.overlay`
- 已预留 future `ContextDirective` schema：
  - `apps/server/src/context/directives/types.ts`
  - 已定义 `create_self_note / pin_node / deprioritize_node / summarize_cluster / archive_overlay`
  - 已预留 trace 字段：
    - `submitted_directives`
    - `approved_directives`
    - `denied_directives`

### 约束保持成立

- 当前 **未** 开放 directive 执行
- 当前 **未** 开放模型直接写 overlay
- 当前 **未** 引入通用 DAG workflow engine
- 当前 **未** 引入前端节点可视化编排器
- 当前 **未** 引入 plugin runtime
- overlay 继续保持 kernel-side working-layer object，而不是 pack runtime source-of-truth

### 验证结果

- `pnpm --filter yidhras-server exec prisma generate` ✅
- `pnpm --filter yidhras-server exec tsc --noEmit` ✅
- `pnpm --filter yidhras-server test:unit -- tests/unit/context_directives_schema.spec.ts tests/unit/context_policy_engine.spec.ts tests/unit/context_module.spec.ts tests/unit/context_orchestrator.spec.ts tests/unit/context_overlay_store.spec.ts` ✅
- `pnpm --filter yidhras-server test:e2e -- tests/e2e/agent-overview.spec.ts tests/e2e/smoke-endpoints.spec.ts` ✅

---

## Multi-model Gateway / Unified AI Task Contract 完成记录

### 已完成实现

- 已新增 `apps/server/src/ai/*` 内部模块边界：
  - `types.ts`
  - `registry.ts`
  - `route_resolver.ts`
  - `task_definitions.ts`
  - `task_service.ts`
  - `gateway.ts`
  - `task_decoder.ts`
  - `observability.ts`
- 已建立服务端内部 canonical contract：
  - `AiTaskService`
  - `RouteResolver`
  - `ModelGateway`
  - provider adapters
- world pack 已支持声明式 `pack.ai` 覆盖：
  - `defaults`
  - `tasks.<task_type>.prompt`
  - `tasks.<task_type>.output`
  - `tasks.<task_type>.parse`
  - `tasks.<task_type>.route`
- 已落地 OpenAI-first：
  - `apps/server/src/ai/providers/openai.ts`
  - 默认 registry 内建 OpenAI provider / model / route
  - 缺少 `OPENAI_API_KEY` 时不影响 `mock / rule_based` 主链启动
- inference 已接入：
  - `GatewayBackedInferenceProvider`
  - 内部 `model_routed` 策略
- 已新增 `AiInvocationRecord` 持久化模型，并与 `InferenceTrace` 通过 `source_inference_id` / `ai_invocation_id` 建立关联证据面

### 约束保持成立

- public `/api/inference/*` 契约当前仍以 `mock | rule_based` 为稳定对外说明
- `model_routed` 当前属于内部/受控能力，不在本阶段作为 public contract 正式开放
- world pack 不允许直接操纵 raw provider payload
- world pack 不允许注入任意可执行 parser/composer 代码
- 更复杂的 AI 行为扩展仍应放在 server-side registered extension

### 验证结果

- `pnpm --filter yidhras-server test:unit -- ai_gateway.spec.ts` ✅
- `pnpm --filter yidhras-server typecheck` ✅
- `smoke-endpoints.spec.ts` 已补 `model_routed` 条件覆盖 ✅
- `inference-workflow-core.spec.ts` 已补 `AiInvocationRecord` 持久化基础验证 ✅

### 当前结论

- 多模型网关底座已在 server 内部成型
- OpenAI-first rollout 已具备真实 provider adapter，不再只是配置占位
- inference 主链已可在不破坏现有 public HTTP 合同的前提下承载统一 AI task/gateway 执行层

---

## AiInvocationRecord 查询/read-model/API surface 完成记录

### 已完成实现

- 新增 read-model / query service：
  - `apps/server/src/app/services/inference_workflow/ai_invocations.ts`
- 新增 inference 路由：
  - `GET /api/inference/ai-invocations`
  - `GET /api/inference/ai-invocations/:id`
- 新增 contracts：
  - `packages/contracts/src/inference.ts` 中的 `aiInvocationsQuerySchema`
  - `aiInvocationIdParamsSchema`
- 已补 integration / smoke 覆盖，验证分页、过滤、详情读取与 workflow trace 中 `ai_invocation_id` 的关联读取路径

### 当前结论

- `AiInvocationRecord` 现已不只是落库证据，而是具备最小只读 observability surface
- 该 surface 仅提供查询，不改变 public inference execution contract，也不意味着 `model_routed` 已正式公开为执行策略
